# Only needed for local development (when running the Python backend separately)
# On Vercel this is not set — the /api/* Python functions handle it automatically
# NEXT_PUBLIC_API_URL=http://localhost:8000
